{
  "name": "dropout-addon",
  "version": "1.9.1",
  "description": "Small quality of life tweaks and watch parties for Dropout.tv",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "build-dev": "node build.js -d",
    "watch": "node build.js -d -w"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "esbuild": "^0.25.10",
    "web-ext": "^8.0.0"
  },
  "dependencies": {
    "cookie": "^1.0.2"
  }
}
